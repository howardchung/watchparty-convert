{
  "name": "watchparty-convert",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "server.ts",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.ts",
    "dev": "node --env-file-if-exists=.env --watch-path . server.ts",
    "prettier": "npx prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
